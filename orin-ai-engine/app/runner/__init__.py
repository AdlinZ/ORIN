"""
ORIN Runner — lightweight agent that runs on user servers.

Responsible for enrollment, periodic heartbeat + resource reporting,
and (in later features) Agent Run lease claiming and execution.
"""
