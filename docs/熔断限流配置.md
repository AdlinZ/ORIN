# 高风险任务熔断与限流配置

> G2.4: 为高风险任务增加熔断、限流或排队提示

---

## 当前已配置

### 现有熔断配置 (application.properties)

```properties
# SiliconFlow Provider 熔断
resilience4j.circuitbreaker.instances.siliconFlow.failure-rate-threshold=50
resilience4j.circuitbreaker.instances.siliconFlow.minimum-number-of-calls=5
resilience4j.circuitbreaker.instances.siliconFlow.wait-duration-in-open-state=10s
resilience4j.circuitbreaker.instances.siliconFlow.permitted-number-of-calls-in-half-open-state=3

# SiliconFlow 重试
resilience4j.retry.instances.siliconFlow.max-attempts=3
```

---

## 建议新增配置

### 高风险任务熔断

```properties
# 知识库解析任务熔断
resilience4j.circuitbreaker.instances.knowledgeParsing.failure-rate-threshold=60
resilience4j.circuitbreaker.instances.knowledgeParsing.wait-duration-in-open-state=30s
resilience4j.circuitbreaker.instances.knowledgeParsing.sliding-window-size=10

# 工作流执行熔断
resilience4j.circuitbreaker.instances.workflowExecution.failure-rate-threshold=70
resilience4j.circuitbreaker.instances.workflowExecution.wait-duration-in-open-state=20s
```

### 限流配置

```properties
# 知识库上传限流
resilience4j.ratelimiter.instances.knowledgeUpload.limit-for-period=10
resilience4j.ratelimiter.instances.knowledgeUpload.limit-refresh-period=60s
resilience4j.ratelimiter.instances.knowledgeUpload.timeout-duration=5s

# AI 调用限流
resilience4j.ratelimiter.instances.aiCall.limit-for-period=100
resilience4j.ratelimiter.instances.aiCall.limit-refresh-period=60s
```

### 超时配置

```properties
# 任务超时配置
task.execution.timeout=300000  # 5分钟
task.parsing.timeout=180000    # 3分钟
task.vectorization.timeout=60000 # 1分钟
```

---

## 排队提示

当前任务队列配置已包含优先级和重试机制：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `orin.task.priority.max` | 10 | 最大优先级 |
| `orin.task.retry.max` | 3 | 最大重试次数 |
| `orin.task.retry.max-interval` | 30000ms | 最大重试间隔 |

---

## 熔断状态前端提示

建议前端增加熔断状态检测：

```javascript
const isCircuitOpen = (error) => {
  return error?.message?.includes('CircuitBreaker') || 
         error?.code === 'CIRCUIT_OPEN'
}

if (isCircuitOpen(error)) {
  ElMessage.warning('服务暂时不可用，请稍后重试')
}
```

---

*最后更新: 2026-03-28*