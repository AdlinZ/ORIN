# ORIN 功能完成度

本文档替代旧的"90%+ 完成度"宣传口径，按当前仓库可见代码与页面给出保守评估，并列出本地可验收能力清单。

## 1. 总体口径

- 平台骨架：约 **75–80%**（多模块入口、数据结构齐备）
- 真实业务闭环：约 **50–60%**（智能体可演示，部分高级链路待补）
- 对外稳定交付：约 **45–55%**（仍需测试体系、稳定性与一致性增强）

判断"能力是否真可用"的统一口径——同时核对：

1. 前端路由与页面是否真实挂载
2. Java 控制器与 service 是否真实闭环
3. Python AI 引擎是否参与并完成真实执行

下列情况均不计为完整交付：仅有页面入口而无后端闭环；控制器存在但 service 占位；Java 入口存在但 Python 端仍是 mock；依赖外部服务且默认配置无法运行。

## 2. 模块成熟度

| 排名 | 模块 | 评分 | 状态 |
|------|------|------|------|
| 1 | 智能体管理 | 8.5 | 接入、列表、对话、网关已成型，可演示 |
| 2 | 系统管理 | 8.2 | 用户、角色、日志、网关基础设施较全 |
| 3 | 知识库 | 6.8 | 上传、解析、检索、同步入口齐备，多源治理不一 |
| 4 | 工作流 | 6.7 | 编辑与管理较完整，复杂节点执行需逐项核对 |
| 5 | 监控观测 | 6.3 | Prometheus + Trace + Langfuse 并存，统一口径在收敛 |
| 6 | 多智能体协作 | 5.2 | 结构完整，真实执行成熟度偏低 |
| 7 | 多模态 | 5.1 | OCR/ASR/视觉已实装，依赖外部配置 |
| 8 | MCP 协议接入 | 8.0 | 已有服务配置、外部工具接入、ORIN Agent 通过 /v1/mcp 对外暴露，并提供 Claude Desktop stdio bridge |
| 9 | 测试与可靠性 | 4.9 | 有测试文件，体系稳定性需加强 |

评分含义：

- `8.0+` 可日常使用，缺陷多为体验优化
- `6.0–7.9` 核心链路可用，存在边界场景与一致性风险
- `4.0–5.9` 页面接口齐备，闭环与运维不足
- `<4.0` 仅展示或局部能力

## 3. 模块说明

### 3.1 智能体管理（8.5）

✅ 已稳定：列表、接入、对话、统一网关  
⚠️ 仍需核对：批量、版本、导入导出能力的 service 实装

### 3.2 知识库（6.8）

✅ 已可用：知识库 CRUD、文档上传、解析、向量化、检索  
⚠️ 风险：不同知识源接入成熟度不一致；图谱构建/检索/前端展示是否形成完整链路待核

### 3.3 工作流（6.7）

✅ 已可用：DSL 验证器、节点处理器、Java 管理层 + Python 执行层  
⚠️ 待补：复杂节点（循环、协作、交互）的执行语义验证

### 3.4 多智能体协作（5.2）

✅ 已落地：任务包/子任务/事件日志数据模型、状态机、LangGraph 编排骨架  
⚠️ 待补：前端人工干预闭环；工作流子任务执行（仍为 TODO 预留）

### 3.5 监控观测（6.3）

✅ 已可用：traceId 透传、Task 监控、Langfuse 接入、告警规则生成  
⚠️ 待补：Langfuse Dashboard 深链；统一观测口径收敛

### 3.6 多模态（5.1）

✅ 已实装：SiliconFlow VLM OCR、ASR（含本地 Whisper 备选）  
⚠️ 风险：依赖外部 API Key，未配置时降级返回错误码

### 3.7 系统管理（8.2）

✅ 已稳定：用户、角色、API Key、网关、模型配置、审计日志、通知

### 3.8 MCP 协议接入（8.0）

✅ 已落地：MCP 服务配置、敏感 env 拒绝、AI Engine 基于官方 SDK 的 tools/list 与 tools/call、协作任务经 TaskRuntime 调用 MCP 工具；ORIN Agent 可通过 `/v1/mcp` 作为 MCP tool 暴露，并可经本地 stdio bridge 接入 Claude Desktop
⚠️ 待补：Workflow 暴露、Agent Skills 市场、更多模板的开箱即用联调

## 4. 测试覆盖率基线

基线测量日期：2026-05-14（PR #7 合并 main 后重测）。当前只记录基线，不设置覆盖率阻断阈值；Phase 2 覆盖率红线从这些数字起步提升。CI 已上传后端、前端、AI Engine 的 coverage artifacts，并在 GitHub Step Summary 中展示覆盖率摘要，供 PR 对比查看。

| 子系统 | 命令 | 语句/指令覆盖率 | 行覆盖率 | 分支覆盖率 |
|--------|------|------------------|----------|------------|
| backend | `mvn -B test jacoco:report` | 17.63% instruction | 17.80% | 12.96% |
| frontend | `npm run test:coverage` | 61.92% statements | 64.25% | 52.74% |
| ai-engine | `python -m pytest --cov=app --cov-report=term-missing --cov-report=xml:coverage.xml`（80 passed） | 51.00% statements | 51.00% | 未记录（未启用 `--cov-branch`） |

> 与 2026-05-13 首次基线相比：backend 略升（16.68→17.63%），frontend 略降（65.00→61.92%，新增未覆盖代码），ai-engine 略升（49.97→51.00%）。

## 5. 本地可验收能力

### 5.0 核心链路验收矩阵

本矩阵是后续调整模块评分、补 smoke、补 targeted test 的入口。本轮只按代码、路由、接口和已有文档建立验收口径，不代表已经完成端到端业务验收。

| 链路 | 前端入口 | 后端入口 | AI Engine 参与点 | 必需依赖 | 当前可验证项 | 当前风险 | 下一步补测 / 修复入口 |
|------|----------|----------|------------------|----------|--------------|----------|------------------------|
| Agent 对话链路 | `ROUTES.AGENTS.WORKSPACE` → `/dashboard/applications/workspace`；`ROUTES.AGENTS.LIST` → `/dashboard/applications/agents` | `POST /api/v1/agents/{agentId}/chat`；会话接口 `POST /api/v1/agents/chat/sessions/{sessionId}/messages` | 默认对话由 Java 后端调 provider；绑定 MCP / 协作任务时经 AI Engine `TaskRuntime` / `mcp_client_manager` 参与，需首轮验收确认实际触发点 | MySQL、Redis、可用模型 provider、Agent API Key / provider key | 前端工作台、Agent 列表、对话 Controller 和会话 Controller 均存在；健康检查可验证后端与数据库 | 外部 provider/key 缺失会导致不可演示；Trace、工具调用、知识库挂载是否完整串起需实测 | 第一条真实验收链路：选一个已配置 Agent，跑普通对话、流式会话、错误语义、traceId 与审计记录 |
| 知识库上传、解析、检索链路 | `ROUTES.KNOWLEDGE.CENTER` → `/dashboard/resources/center`；`ROUTES.KNOWLEDGE.ASSETS` → `/dashboard/resources/assets`；`ROUTES.KNOWLEDGE.RETRIEVAL_LAB` → `/dashboard/resources/retrieval` | `POST /api/v1/knowledge`；`POST /api/v1/knowledge/{kbId}/documents/upload`；`POST /api/v1/knowledge/documents/{docId}/parse`；`POST /api/v1/knowledge/documents/{docId}/vectorize`；`POST /api/v1/knowledge/retrieve/test` | 普通上传、解析、向量化、检索以 Java 后端为主；Workflow 的 `knowledge_retrieval` 节点经 AI Engine `app.engine.executor` 参与 | MySQL、Milvus、Embedding Provider、至少一个已入库文档；图谱链路另依赖图谱构建数据 | CRUD、上传、解析、向量化、检索测试、任务查询 Controller 均存在；前端知识检索与知识资产入口已挂载 | Milvus / embedding provider 未配置时链路不可闭环；多源同步成熟度不一；知识图谱构建/检索/展示仍需单列验收 | 用最小文档跑上传 → 解析 → 向量化 → 检索；补 targeted test 覆盖无 provider、无 Milvus、空文档错误语义 |
| Workflow 创建、执行、查看结果链路 | `ROUTES.AGENTS.WORKFLOWS` → `/dashboard/applications/workflows`；`ROUTES.AGENTS.WORKFLOW_EXECUTION` → `/dashboard/applications/workflows/execution`；`ROUTES.AGENTS.WORKFLOW_VISUAL` → `/dashboard/applications/workflows/visual` | 管理接口 `/api/workflows/*`；执行代理 `POST /api/v1/workflow/run`；实例查询 `GET /api/workflows/instances/{instanceId}` | `orin-ai-engine/app/api/workflow.py` → `WorkflowExecutor.execute()`；节点处理器在 `app.engine.nodes` / `app.engine.executor` | MySQL、Redis、AI Engine；含 LLM / knowledge / MCP 节点时还需对应 provider 或服务 | Java 管理、发布、执行、实例查询接口存在；Python workflow router 与 executor 存在；前端工作流中心与执行记录入口已挂载 | 复杂节点（循环、协作、人工交互）执行语义需逐项核对；Java 执行代理与 Python 执行结果字段一致性需实测 | 先跑最小 DSL，再逐步覆盖 LLM、条件、知识检索、MCP、协作节点；补实例状态与错误语义测试 |
| Collaboration 包创建、分解、执行、结果回传链路 | `ROUTES.AGENTS.COLLABORATION_WORKFLOWS` → `/dashboard/applications/collaboration/workflows`；`ROUTES.AGENTS.COLLABORATION_DASHBOARD` → `/dashboard/applications/collaboration/dashboard` | `POST /api/v1/collaboration/packages`；`POST /api/v1/collaboration/packages/{packageId}/decompose`；`GET /api/v1/collaboration/packages/{packageId}/subtasks`；`GET /api/v1/collaboration/events/{packageId}`；子任务执行 / 重试 / 跳过 / 人工完成接口 | 编排层 `app.engine.collaboration_langgraph`；分发层 `app.engine.mq_worker`；执行内核 `TaskRuntime` | MySQL、Redis、AI Engine；队列增强依赖 RabbitMQ 可降级；子任务类型依赖对应 Agent / Workflow / MCP 配置 | 包、子任务、事件、黑板、checkpoint、人工干预 Controller 较全；AI Engine 单一执行内核存在；前端协作入口已挂载 | 前端人工干预按钮与详情闭环仍未完成；工作流子任务执行仍有预留实现；RabbitMQ 未启动时只能按降级口径验收 | 以一个包完成创建 → 分解 → 执行 Agent 子任务 → 事件回显 → 审计记录；另列工作流子任务和人工干预专项 |
| MCP tools/list、tools/call、ORIN Agent 暴露链路 | `ROUTES.SYSTEM.SETTINGS_MCP_SERVICE` → `/dashboard/control/mcp-service`；`ROUTES.AGENTS.MCP` → `/dashboard/applications/extensions?tab=mcp` | ORIN Agent 暴露 `GET/POST /v1/mcp`；服务配置 `/api/system/mcp/services`；工具列表 `/api/system/mcp/tools`；AI Engine MCP API `/api/mcp/*` | `orin-ai-engine/app/api/mcp.py`；`app.engine.mcp_client_manager`；协作 / MQ 执行经 `TaskRuntime.execute_mcp_task` | MySQL、Redis、可达 MCP 服务（stdio / SSE / HTTP）；工具调用所需 env / secrets | MCP 服务配置、敏感 env 拒绝、tools/list、tools/call、ORIN Agent 通过 `/v1/mcp` 暴露均有代码入口；Claude Desktop stdio bridge 已记录 | 模板开箱联调不足；更多工具场景、错误语义、超时与鉴权边界需验收；Workflow 暴露仍是后续项 | 用一个本机 stdio MCP 服务跑连接测试、tools/list、tools/call，再验证 `/v1/mcp` 作为外部协议入口 |

### 5.1 已完成（浏览器即验）

| 能力 | 路径 / 命令 | 通过口径 |
|------|-------------|----------|
| 前端入口 | `/`、`/portal` | 页面打开，无白屏 |
| 后端健康检查 | `GET /v1/health`、`GET /api/v1/health` | 无需 API Key，返回服务状态 |
| AI Engine 健康 | `GET :8000/health`、`GET :8000/v1/health` | 返回 `status` / `service` / `dependencies` |
| 旧路由兼容 | 见 `routes.js` 的 `LEGACY_ROUTE_REDIRECTS` | 历史链接可跳转到当前入口 |
| 鉴权提示 | `/dashboard/system/security-ops` | 统一网关入口明确提示 API Key 要求 |
| 工作流首屏 | 侧边栏“智能体管理 → 工作流中心” | 页面打开后工作流入口无白屏 |

### 5.2 可联调（需依赖）

| 能力 | 路径 | 依赖 |
|------|------|------|
| 知识库检索 | 侧边栏“知识库管理 → 知识检索” | MySQL · Milvus · Embedding Provider · 至少一个已入库文档 |
| 工作流执行 | 侧边栏“智能体管理 → 工作流中心” | 后端工作流 + AI Engine |
| 智能体对话 + Trace | `/dashboard/agents/manage` → 控制台 | 可用模型 provider 与 API Key |
| 统一网关统计 | `/dashboard/system/security-ops` | 真实流量 + Redis |
| MCP 连接测试 | `/dashboard/applications/extensions?tab=mcp` | 本机 STDIO 命令或可达 SSE 地址 |

### 5.3 预留中（页面存在但能力未闭环）

| 能力 | 当前口径 |
|------|----------|
| MCP 工具市场完整工具列表 | 模板已展示，真实工具列表依赖具体服务接入 |
| 外部 provider 全量可用性 | 不默认可用，缺 key/模型时按统一错误语义返回 |
| RabbitMQ 协作队列 | 本地默认不强制启动，健康检查暴露 `rabbitmq` 依赖状态 |
| 知识图谱完整链路 | 实体已落地，端到端构建/检索/展示协同待完善 |
| 协作人工干预 | 后端接口已实装，前端按钮闭环未完成 |

## 6. 烟测命令

```bash
bash scripts/smoke-test.sh

# 三端启动后人工抽查
curl -fsS http://127.0.0.1:8080/v1/health
curl -fsS http://127.0.0.1:8080/api/v1/health
curl -fsS http://127.0.0.1:8000/health
curl -fsS http://127.0.0.1:8000/v1/health
curl -i http://127.0.0.1:8080/v1/models   # 期望 401 + AUTH_API_KEY_INVALID
```

## 7. 待补清单

详细 P0/P1/P2 与进度跟踪见 [../TODO.md](../TODO.md)。摘要：

**P0 · 核心闭环**

- 协作页人工干预按钮与详情闭环
- 协作子任务 ↔ 工作流执行真实映射
- 复杂工作流节点执行语义验证
- 前端集成测试体系

**P1 · 重要增强**

- Langfuse 深链与 Dashboard 入口
- 知识图谱端到端链路（构建/检索/展示）
- Dify / RAGFlow 同步稳定性统一
- 系统设置 / 帮助 / 消息中心收口

**P2 · 体验优化**

- 日 / 周 / 月报导出
- 趋势预测与资源预算
- 路由清理与别名收敛
