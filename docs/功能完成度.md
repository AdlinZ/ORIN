# ORIN 功能完成度

本文档替代旧的"90%+ 完成度"宣传口径，按 2026-07-31 的 `main` 代码、干净测试和本机运行态给出保守评估。根目录 `VERSION` 和当前 `main` 是版本与功能真相源；历史路线图勾选、预发布标签或已删除代码不能单独作为交付证据。

## 1. 总体口径

- 产品定位：面向企业 AI 应用管理员的智能体与知识库统一管理平台
- 核心资产：智能体、知识库、模型 Provider
- 高级能力：Workflow、MCP、多智能体协作、知识图谱、多模态
- 平台骨架：约 **75–80%**（多模块入口、数据结构齐备）
- 真实业务闭环：约 **55–65%**（智能体与知识库入口齐备，但真实 Provider + Milvus + 知识绑定回答的黄金流程 E2E 仍待补；Workflow / Collaboration / MCP 已有独立 API smoke）
- 对外稳定交付：约 **50–55%**（CI、Docker quickstart、业务 smoke 与安全基线已建立，覆盖率红线、真实业务 E2E、最终 release 仍待补）

判断"能力是否真可用"的统一口径——同时核对：

1. 前端路由与页面是否真实挂载
2. Java 控制器与 service 是否真实闭环
3. Python AI 引擎是否参与并完成真实执行

下列情况均不计为完整交付：仅有页面入口而无后端闭环；控制器存在但 service 占位；Java 入口存在但 Python 端仍是 mock；依赖外部服务且默认配置无法运行。

### 1.1 当前版本与恢复边界

- 当前产品线统一为 `0.2.0`，尚未创建最终 `v0.2.0` tag；`v0.2.0-alpha.1` 与 `v0.2.0-submission.*` 是历史预发布标签。
- 2026-07-30 的产品基线恢复已合并 `main`，因此当前代码优先于恢复前的功能文档。
- Runner 管理、Agent draft/freeze/不可变版本、备份恢复脚本、审计导出、覆盖率 gate、未收口的扩展 OTel 链路与若干复杂 Java Workflow 节点处理器不属于当前交付基线。
- 如需恢复上述能力，必须重新满足代码、权限、测试、文档和真实验收闭环，不沿用历史“已完成”标记。

## 2. 模块成熟度

下表评分表示当前代码成熟度，不表示产品导航或开发优先级。智能体和知识库始终是产品核心；MCP 即使实现成熟度较高，也属于增强能力。

| 排名 | 模块 | 评分 | 状态 |
|------|------|------|------|
| 1 | 智能体管理 | 8.6 | 接入、列表、对话、网关与运维工作台已成型，发布/归属语义可核对 |
| 2 | 系统管理 | 8.5 | 用户、角色、日志、网关、API Key 生命周期基础设施较全，首版角色矩阵、管理接口权限收口与 API Key 自助边界已落地 |
| 3 | 知识库 | 7.0 | 上传、解析、检索、同步与归属字段齐备，多源治理不一 |
| 4 | 工作流 | 6.8 | 编辑与管理较完整，发布校验已拒绝非法图，复杂节点执行仍需逐项核对 |
| 5 | 监控观测 | 6.3 | Prometheus + Trace + Langfuse 并存，统一口径在收敛 |
| 6 | 多智能体协作 | 6.2 | 结构完整，Workflow 子任务强验收与协作看板浏览器 E2E 已具备，真实 Agent/MCP 子任务样本仍待补 |
| 7 | 多模态 | 5.1 | OCR/ASR/视觉已实装，依赖外部配置 |
| 8 | MCP 协议接入 | 8.4 | 已有服务配置、外部工具接入、ORIN Agent / Workflow 通过 /v1/mcp 对外暴露，并提供 Codex-first 客户端验收清单、stdio bridge 与 open demo smoke |
| 9 | 测试与可靠性 | 5.5 | 三端测试、构建和浏览器 smoke 当前通过，但真实业务 E2E 与覆盖率红线仍缺 |

评分含义：

- `8.0+` 可日常使用，缺陷多为体验优化
- `6.0–7.9` 核心链路可用，存在边界场景与一致性风险
- `4.0–5.9` 页面接口齐备，闭环与运维不足
- `<4.0` 仅展示或局部能力

## 3. 模块说明

### 3.1 智能体管理（8.6）

✅ 已稳定：列表、接入、对话、统一网关；`ROLE_OPERATOR` 默认进入运维工作台；菜单收敛为运维工作台/列表/会话记录；非管理员按 `owner_user_id` 过滤智能体读写；运维工作台含绑定/调试/API Key 用量摘要  
⚠️ 仍需核对：批量、版本、导入导出能力的 service 实装；跨用户共享/委托 ACL

### 3.2 知识库（7.0）

✅ 已可用：知识库 CRUD、文档上传、解析、向量化、检索；知识库工作台已展示真实处理阶段、自动轮询后台状态、Milvus/Embedding 诊断和检索降级原因；V91 起单独持久化脱敏后的向量化失败原因；V92 起知识库具备 `owner_user_id` 归属盖章，列表与文档读写按归属过滤（管理员全局）
⚠️ 风险：真实依赖下的上传到检索 smoke/E2E 需定期跑；不同知识源接入成熟度不一致；同步入库库可能归系统管理员；图谱完整链路待核

### 3.3 工作流（6.8）

✅ 已可用：DSL 验证器、非法图拒绝（环、孤岛、不可达、无终点路径）、节点处理器、Java 管理层 + Python 执行层
⚠️ 待补：复杂节点（协作、交互）的执行语义验证

### 3.4 多智能体协作（6.2）

✅ 已落地：任务包/子任务/事件日志数据模型、状态机、LangGraph 编排骨架；Workflow 子任务运行态 smoke 已在本机临时 `LANGGRAPH_MQ` 后端、AI Engine MQ worker 和隔离 RabbitMQ 队列下通过；前端协作详情已接 runtime、diagnostics、events、subtasks 与人工干预入口；Playwright E2E 已覆盖协作看板包级 pause/resume/manual-complete 和子任务 retry/skip/manual-complete 的浏览器交互与刷新；critic 驳回后的 FALLBACK 真重派已能重置子任务、清理旧 branch_result 并回到既有执行链
⚠️ 待补：真实后端联调下的人工干预浏览器验收；真实 Agent / MCP 子任务样本；常驻本机多后端同时运行时需隔离 RabbitMQ queue，避免验收消息被其他消费者抢占

### 3.5 监控观测（6.3）

✅ 已可用：traceId 透传、Task 监控、Langfuse 接入、告警规则生成、前端错误提示保留 traceId
⚠️ 待补：Langfuse Dashboard 深链；统一观测口径收敛

### 3.6 多模态（5.1）

✅ 已实装：SiliconFlow VLM OCR、ASR（含本地 Whisper 备选）  
⚠️ 风险：依赖外部 API Key，未配置时降级返回错误码

### 3.7 系统管理（8.5）

✅ 已稳定：用户、角色、API Key、网关、模型配置、审计日志、通知；`CLIENT_ACCESS / sk-orin-*` 平台访问密钥支持创建、禁用、启用、删除、轮换、调用摘要、最近历史与脱敏审计，前端可复制 MCP 客户端配置；首版角色矩阵已同步到前端菜单、路由守卫、用户/部门/角色管理接口权限边界和 `/portal/api-keys` 自助入口
⚠️ 待补：按 API Key 的长期配额趋势、限流命中明细、租户隔离与跨用户共享 ACL

### 3.8 MCP 协议接入（8.4）

✅ 已落地：MCP 服务配置、敏感 env 拒绝、AI Engine 基于官方 SDK 的 tools/list 与 tools/call、协作任务经 TaskRuntime 调用 MCP 工具；ORIN Agent / Workflow 可通过 `/v1/mcp` 作为 MCP tool 暴露，并可经本地 stdio bridge 或兼容 Streamable HTTP 客户端接入；仓库提供 `scripts/mcp-open-demo-smoke.sh` 和 `scripts/open-demo-acceptance.sh` 做 initialize / tools/list / Workflow tools/call / trace metadata 验收，设置 `ORIN_OPEN_DEMO_AGENT_ID` 后可将真实 provider-backed Agent MCP `tools/call` 纳入必过验收；Codex CLI 子会话已通过真实 MCP 配置调用 Workflow tool 和临时 provider-backed Ollama Agent tool
⚠️ 待补：Agent Skills 市场、更多模板的开箱即用联调、Claude Desktop / Cursor / Windsurf 外部客户端展示资产

## 4. 当前测试与覆盖率

测量日期：2026-07-31，基于当前工作区执行干净后端构建、前端测试与 AI Engine coverage。当前只记录基线，不设置覆盖率阻断阈值；CI 上传三端 coverage artifacts，并在 GitHub Step Summary 中展示摘要。

| 子系统 | 命令与结果 | 语句/指令覆盖率 | 行覆盖率 | 分支覆盖率 |
|--------|------------|------------------|----------|------------|
| backend | `mvn clean test`（559 passed） | 21.08% instruction | 21.03% | 15.46% |
| frontend | `npm run test:coverage`（130 passed） | 57.70% statements | 59.13% | 55.80% |
| ai-engine | `pytest --cov=app`（86 passed） | 51.00% statements | 51.00% | 未记录（未启用 `--cov-branch`） |

补充验证：`npm run build` 通过；Playwright 13 个浏览器 smoke 通过。浏览器 smoke 主要验证路由、无白屏、角色导航和 mock/API 拦截交互，不能替代真实 Provider、Milvus、RabbitMQ 或真实后端业务 E2E。

## 5. 本地可验收能力

### 5.0 核心链路验收矩阵

本矩阵是后续调整模块评分、补 smoke、补 targeted test 的入口。API 级 smoke 与 open demo smoke 只能证明“已具备自动验收入口”；真实浏览器 E2E、桌面客户端、外部 provider 场景仍需单独标注，不得被历史记录或 mock smoke 代替。

| 链路 | 前端入口 | 后端入口 | AI Engine 参与点 | 必需依赖 | 当前可验证项 | 当前风险 | 下一步补测 / 修复入口 |
|------|----------|----------|------------------|----------|--------------|----------|------------------------|
| Agent 对话链路 | `ROUTES.AGENTS.WORKSPACE` → `/dashboard/applications/workspace`；`ROUTES.AGENTS.LIST` → `/dashboard/applications/agents` | `POST /api/v1/agents/{agentId}/chat`；会话接口 `POST /api/v1/agents/chat/sessions/{sessionId}/messages`；绑定 `PUT /api/v1/agent-tools/bindings/agents/{agentId}` | 默认对话由 Java 后端调 provider；绑定 MCP / 协作任务时经 AI Engine `TaskRuntime` / `mcp_client_manager` 参与 | MySQL、Redis、可用模型 provider、Agent API Key / provider key；知识回答另需 Milvus + Embedding | 工作台 RAG + `retrievedChunks` / citation chips；workspace chat 审计与响应含 `traceId`；发布口径为 `mcpExposed`；黄金流程 smoke 可断言绑定后 session 回答 | 外部 provider/key 缺失会导致不可演示；简单 `/chat` 路径不含 RAG | `ORIN_BUSINESS_SMOKE_KNOWLEDGE_GOLDEN=1` + `ORIN_BUSINESS_SMOKE_AGENT_ID`；手工抽查发布后 `/v1/mcp` 调用 |
| 知识库上传、解析、检索链路 | `ROUTES.KNOWLEDGE.CENTER` → `/dashboard/resources/center`；`ROUTES.KNOWLEDGE.ASSETS` → `/dashboard/resources/assets`；`ROUTES.KNOWLEDGE.RETRIEVAL_LAB` → `/dashboard/resources/retrieval` | `POST /api/v1/knowledge`；`POST /api/v1/knowledge/{kbId}/documents/upload`；`POST /api/v1/knowledge/documents/{docId}/parse`；`POST /api/v1/knowledge/documents/{docId}/vectorize`；`POST /api/v1/knowledge/retrieve/test`；`GET /api/v1/knowledge/diagnose/milvus` | 普通上传、解析、向量化、检索以 Java 后端为主；Workflow 的 `knowledge_retrieval` 节点经 AI Engine `app.engine.executor` 参与 | MySQL、Milvus、Embedding Provider、至少一个已入库文档；图谱链路另依赖图谱构建数据 | CRUD、上传、解析、向量化、检索测试、任务查询 Controller 均存在；前端知识检索与知识资产入口已挂载；`diagnose/milvus` 返回结构化 `milvus` / `embedding` / `documentPipeline`；`ORIN_BUSINESS_SMOKE_KNOWLEDGE_GOLDEN=1` 可跑创建→上传→解析→向量化→检索半程；叠加 `ORIN_BUSINESS_SMOKE_AGENT_ID` 可继续绑定并断言 workspace `retrievedChunks` + Trace | Milvus / embedding provider 未配置时链路不可闭环；多源同步成熟度不一；知识图谱构建/检索/展示仍需单列验收 | 在真实依赖环境定期跑 `ORIN_BUSINESS_SMOKE_KNOWLEDGE_GOLDEN=1`（完整链路再加 `ORIN_BUSINESS_SMOKE_AGENT_ID`） |
| Workflow 创建、执行、查看结果链路 | `ROUTES.AGENTS.WORKFLOWS` → `/dashboard/applications/workflows`；`ROUTES.AGENTS.WORKFLOW_EXECUTION` → `/dashboard/applications/workflows/execution`；`ROUTES.AGENTS.WORKFLOW_VISUAL` → `/dashboard/applications/workflows/visual` | 管理接口 `/api/workflows/*`；执行代理 `POST /api/v1/workflow/run`；实例查询 `GET /api/workflows/instances/{instanceId}`；任务恢复 `/api/v1/workflow-tasks/{taskId}/replay|cancel` | `orin-ai-engine/app/api/workflow.py` → `WorkflowExecutor.execute()`；节点处理器在 `app.engine.nodes` / `app.engine.executor` | MySQL、Redis、AI Engine；含 LLM / knowledge / MCP 节点时还需对应 provider 或服务 | Java 管理、发布、执行、实例查询接口存在；Workflow task 状态语义、取消、重放和任务历史入口已闭环；前端工作流执行页可直接查看历史并恢复失败任务 | 复杂节点（循环、协作、人工交互）执行语义需逐项核对；Java 执行代理与 Python 执行结果字段一致性需实测 | 先跑最小 DSL，再逐步覆盖 LLM、条件、知识检索、MCP、协作节点；补实例状态与错误语义测试 |
| Collaboration 包创建、分解、执行、结果回传链路 | `ROUTES.AGENTS.COLLABORATION_WORKFLOWS` → `/dashboard/applications/collaboration/workflows`；`ROUTES.AGENTS.COLLABORATION_DASHBOARD` → `/dashboard/applications/collaboration/dashboard` | `POST /api/v1/collaboration/packages`；`POST /api/v1/collaboration/packages/{packageId}/decompose`；`POST /api/v1/collaboration/packages/{packageId}/fallback/retry`；`GET /api/v1/collaboration/packages/{packageId}/subtasks`；`GET /api/v1/collaboration/events/{packageId}`；runtime / diagnostics / pause / resume / cancel / manual-complete；子任务执行 / 重试 / 跳过 / 人工完成接口 | 编排层 `app.engine.collaboration_langgraph`；分发层 `app.engine.mq_worker`；执行内核 `TaskRuntime` | MySQL、Redis、AI Engine；队列增强依赖 RabbitMQ 可降级；子任务类型依赖对应 Agent / Workflow / MCP 配置 | 包、子任务、事件、黑板、checkpoint、人工干预 Controller 较全；AI Engine 单一执行内核存在；显式 `workflowId` 子任务已映射到 MQ `WORKFLOW` 策略并由 `TaskRuntime.execute_workflow_task` 执行；critic 驳回后的 FALLBACK 真重派可重置子任务、清理旧 branch_result、记录重派事件并回到既有执行链；`business-smoke.sh` 已提供可选端到端 Workflow 子任务验收；`open-demo-acceptance.sh` 的强验收已在隔离 RabbitMQ queue 下跑通 Collaboration Workflow 子任务；前端协作详情已展示 runtime、diagnostics、events、subtasks 和人工干预操作；Playwright E2E 已覆盖 mock 后端下的浏览器交互刷新 | RabbitMQ 未启动时只能按降级口径验收；Workflow 子任务运行态验收需 AI Engine 配置后端授权；真实后端联调下的浏览器人工干预验收仍缺 | 继续补真实 Agent / MCP 子任务样本与真实后端浏览器 E2E |
| MCP tools/list、tools/call、ORIN Agent 暴露链路 | `ROUTES.SYSTEM.SETTINGS_MCP_SERVICE` → `/dashboard/control/mcp-service`；`ROUTES.AGENTS.MCP` → `/dashboard/applications/extensions?tab=mcp` | ORIN Agent / Workflow 暴露 `GET/POST /v1/mcp`；服务配置 `/api/system/mcp/services`；工具列表 `/api/system/mcp/tools`；AI Engine MCP API `/api/mcp/*` | `orin-ai-engine/app/api/mcp.py`；`app.engine.mcp_client_manager`；协作 / MQ 执行经 `TaskRuntime.execute_mcp_task` | MySQL、Redis、可达 MCP 服务（stdio / SSE / HTTP）；工具调用所需 env / secrets | MCP 服务配置、敏感 env 拒绝、tools/list、tools/call、ORIN Agent / Workflow 通过 `/v1/mcp` 暴露均有代码入口；API Key / Origin / owner 隔离、batch 拒绝、trace/package 与审计已有后端测试；Codex CLI 子会话已通过临时 `orin` MCP 配置调用 Workflow tool，并通过临时 `orin-agent` MCP 配置调用 provider-backed Ollama Agent tool；`open-demo-acceptance.sh` 可自动创建 exposed Workflow 与临时 MCP key并完成 Workflow `tools/call`，传入 `ORIN_OPEN_DEMO_AGENT_ID` 时可强验收真实 provider-backed Agent `tools/call` | 模板开箱联调不足；Claude Desktop / Cursor / Windsurf 外部客户端展示需单独跑通；更多工具场景、错误语义、超时与真实客户端兼容需持续验收 | 补 Claude Desktop / Cursor / Windsurf 展示截图或 GIF；将 Codex Agent 验收步骤沉淀为可复用脚本或清单 |

`scripts/business-smoke.sh` 是 Phase 1A–1E 的 API 级验收入口，覆盖 API Key 生命周期（临时 `CLIENT_ACCESS` key 创建、`/v1/mcp` 成功、禁用后 401、清理）、Agent 列表、Workflow 最小 DSL 创建/发布/提交/查询、Workflow trace summary 聚合断言、已完成任务的取消/重放保护、失败 Workflow replay、Collaboration 创建/分解/查询、Collaboration trace summary 聚合断言。脚本默认自动探测 `RABBITMQ_HOST:RABBITMQ_PORT`，RabbitMQ 可达时要求 Workflow task 跑到 `COMPLETED`，未启 RabbitMQ 时才按 `WARN` 降级；`docker-smoke.sh` 额外强制启用严格口径。Agent chat 仍需显式配置 `ORIN_BUSINESS_SMOKE_AGENT_ID`，设置后会强校验响应语义与 trace summary 关联记录。协作 Workflow 子任务默认跳过；设置 `ORIN_BUSINESS_SMOKE_WORKFLOW_SUBTASK=1` 后，会要求显式 `workflowId` 子任务经 AI Engine `TaskRuntime` 返回 Workflow enqueue 结果，并在同一 trace summary 中查到 collaboration package 与 workflow task。`scripts/open-demo-acceptance.sh` 用于开源演示验收，可自动创建临时 exposed Workflow 与 MCP key；设置 `ORIN_OPEN_DEMO_RUN_WORKFLOW_SUBTASK=1` 后可强验收 Collaboration Workflow 子任务。

### 5.1 已完成（浏览器即验）

| 能力 | 路径 / 命令 | 通过口径 |
|------|-------------|----------|
| 前端入口 | `/`、`/portal` | 页面打开，无白屏 |
| 后端健康检查 | `GET /v1/health`、`GET /api/v1/health` | 无需 API Key，返回服务状态 |
| AI Engine 健康 | `GET :8000/health`、`GET :8000/v1/health` | 返回 `status` / `service` / `dependencies` |
| 旧路由兼容 | 见 `routes.js` 的 `LEGACY_ROUTE_REDIRECTS` | 历史链接可跳转到当前入口 |
| 鉴权提示 | `/dashboard/system/security-ops` | 统一网关入口明确提示 API Key 要求 |
| API Key 生命周期 | `/dashboard/control/gateway?workspace=access`、`/portal/api-keys` | 管理员全局治理；普通用户 / 运维自助管理本人 `CLIENT_ACCESS` Key；创建、禁用、启用、删除、轮换、MCP 配置复制、调用摘要与最近历史不泄漏完整请求 / 响应 |
| 工作流首屏 | “高级能力 → 工作流中心” | 页面打开后工作流入口无白屏 |

### 5.2 可联调（需依赖）

| 能力 | 路径 | 依赖 |
|------|------|------|
| 知识库检索 | “知识库 → 知识检索” | MySQL · Milvus · Embedding Provider · 至少一个已入库文档 |
| 工作流执行 | “高级能力 → 工作流中心” | 后端工作流 + AI Engine |
| 智能体对话 + Trace | `/dashboard/agents/manage` → 控制台 | 可用模型 provider 与 API Key |
| 统一网关统计 | `/dashboard/system/security-ops` | 真实流量 + Redis |
| MCP 连接测试 | `/dashboard/applications/extensions?tab=mcp` | 本机 STDIO 命令或可达 SSE 地址 |

### 5.3 预留中（页面存在但能力未闭环）

| 能力 | 当前口径 |
|------|----------|
| MCP 工具市场完整工具列表 | 模板已展示，真实工具列表依赖具体服务接入 |
| 外部 provider 全量可用性 | 不默认可用，缺 key/模型时按统一错误语义返回 |
| RabbitMQ 队列 | Docker quickstart 默认启动，用于 Workflow task 完整消费；本机进程模式可选 |
| Workflow task 运行态 | wire status 固定为 `QUEUED / RUNNING / RETRYING / COMPLETED / FAILED / DEAD / CANCELLED`；`CANCELLED` 是终态，`QUEUED` 可取消，`FAILED / DEAD` 可重放 |
| 知识图谱完整链路 | 实体已落地，端到端构建/检索/展示协同待完善 |
| 协作人工干预 | 后端接口已实装，前端协作 Dashboard 已提供包级 pause/resume/cancel/manual-complete 与子任务 retry/skip/manual-complete 入口 |

## 6. 烟测命令

```bash
bash scripts/smoke-test.sh

# 三端启动后跑核心业务 API 验收；会创建并尽力清理临时 Workflow / Collaboration 数据
bash scripts/business-smoke.sh

# 三端启动后人工抽查
curl -fsS http://127.0.0.1:8080/v1/health
curl -fsS http://127.0.0.1:8080/api/v1/health
curl -fsS http://127.0.0.1:8000/health
curl -fsS http://127.0.0.1:8000/v1/health
curl -i http://127.0.0.1:8080/v1/models   # 期望 401 + AUTH_API_KEY_INVALID
```

`business-smoke.sh` 覆盖登录、临时 API Key 创建、`/v1/mcp` 鉴权成功、禁用后 401、Agent 列表、Workflow 最小 DSL 创建/发布/提交/查询、Workflow trace summary 聚合断言、已完成任务恢复保护、失败 Workflow replay、Collaboration 创建/分解/查询、Collaboration trace summary 聚合断言。Agent chat 默认 `SKIP`，需 `ORIN_BUSINESS_SMOKE_AGENT_ID=<agentId>` 才纳入必检；纳入后会要求响应不是错误负载，并在 trace summary 中查到审计或 trace 记录。协作 Workflow 子任务默认 `SKIP`，需 `ORIN_BUSINESS_SMOKE_WORKFLOW_SUBTASK=1` 才纳入必检；纳入后要求 AI Engine MQ worker、后端协作结果监听与 `ORIN_BACKEND_AUTHORIZATION` 已在服务启动时配置。`ORIN_BUSINESS_SMOKE_REQUIRE_WORKFLOW_COMPLETED` 默认为 `auto`，即 RabbitMQ 可达时要求 Workflow task 到 `COMPLETED`，也可显式设为 `true` / `false` 覆盖。

## 7. 待补清单

详细 P0/P1/P2 与进度跟踪见 [../TODO.md](../TODO.md)。摘要：

**P0 · 核心闭环**

- 知识库半程与知识→绑定→workspace 回答黄金流程已纳入 `business-smoke.sh`（`ORIN_BUSINESS_SMOKE_KNOWLEDGE_GOLDEN` / `ORIN_BUSINESS_SMOKE_AGENT_ID`，默认 SKIP）；简单 Agent chat 仍可用 `ORIN_BUSINESS_SMOKE_AGENT_ID` 单独验收；Workflow / Collaboration trace 聚合端到端断言已纳入 `business-smoke.sh`
- 协作子任务 ↔ Workflow 执行真实映射已补 Java 显式分解、MQ 策略映射、AI Engine `TaskRuntime` 单测与 open demo 强验收样本；协作看板人工干预已补 Playwright 浏览器 E2E；FALLBACK 真重派已补自动闭环；后续补真实 Agent / MCP 子任务样本与真实后端联调验收
- 复杂工作流节点执行语义验证；DSL 发布校验已拒绝环、孤岛、不可达节点、无终点路径与非法边引用
- MCP open demo 已覆盖 Workflow `tools/call`，并可通过 `ORIN_OPEN_DEMO_AGENT_ID` 强验收真实 provider-backed Agent MCP `tools/call`；Codex Workflow tool 与 provider-backed Agent tool 客户端验收已通过，Claude Desktop / Cursor / Windsurf 展示仍缺
- 前端集成测试体系；当前已补协作干预、任务恢复、Trace 聚合与错误 traceId 提示的单元测试入口，并新增协作看板 Playwright E2E
- Phase 1.5 角色化体验已补首版角色矩阵、菜单 / 路由同源过滤、用户/部门/角色管理接口权限测试和 API Key 自助边界；后续补资源级权限

**P1 · 重要增强**

- Phase 1.5 角色化体验：角色矩阵、权限同源、默认页、专属视图、越权测试
- Langfuse 深链与 Dashboard 入口
- API Key 长期配额趋势与限流命中明细
- Agent Skills 市场、Skills YAML 字段、示例 Skills
- 知识图谱关系抽取产出率与统一 LLM gateway 接入评估

**P2 · 体验优化**

- Phase 2 横切治理：错误码、`traceparent`、JSON 日志、OTel / Jaeger、覆盖率红线
- Phase 3 安全运维：默认凭据移除、API Key 过期 / 自动轮换、审计导出、备份恢复
- Phase 4 社区化：最终 `v0.2.0` release、Docker 镜像、README 截图 / GIF、展示资产
