# ORIN 开发者手册

本文档面向 ORIN 平台的开发者，涵盖本地调试、API 接入、MCP 集成与代码规范。

---

## 1. 本地开发环境

### 1.1 快速启动（进程模式）

```bash
# 前置依赖：JDK 17+、Node 18+、Python 3.11-3.14、MySQL 8、Redis
# 启动全部服务（后端 + 前端 + AI Engine）
./manage.sh start

# 查看服务状态
./manage.sh status

# 停止全部服务
./manage.sh stop

# 重启某个服务
./manage.sh restart backend
```

### 1.2 Docker 模式开发

```bash
# 仅启动基础设施（MySQL / Redis / RabbitMQ），后端和前端用进程模式
docker compose up -d mysql redis rabbitmq

# 验证基础设施
bash scripts/check-docker-quickstart.sh

# 分别启动后端和前端
cd orin-backend && mvn spring-boot:run
cd orin-frontend && npm run dev
```

### 1.3 环境变量

关键环境变量（`.env`）：

```bash
# 数据库（必须）
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=your_password
REDIS_HOST=localhost
REDIS_PASSWORD=

# AI Engine（必须）
ORIN_AI_ENGINE_URL=http://localhost:8000

# 后端（必须）
ORIN_DEFAULT_ADMIN_PASSWORD=your_admin_password
JWT_SECRET=your_jwt_secret_min_32_chars

# 可选：启用 OTel 链路追踪
ORIN_TRACING_ENABLED=true
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# 可选：Langfuse LLM 观测
LANGFUSE_ENABLED=true
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:3000
```

---

## 2. API 接入

### 2.1 获取 API Key

1. 登录管理台 → `/dashboard/control/gateway` → 访问凭据
2. 创建新密钥，复制 `sk-orin-...` 前缀的密钥
3. 密钥仅显示一次，务必保存

### 2.2 API 调用示例

```bash
# 设置变量
API_KEY="sk-orin-your-key-here"
BASE_URL="http://localhost:8080"

# 1. 登录获取 JWT（或者使用 API Key 直接访问）
curl -X POST "${BASE_URL}/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"your_password"}'

# 2. Agent 列表（API Key 鉴权）
curl "${BASE_URL}/api/v1/agents" \
  -H "Authorization: Bearer ${API_KEY}"

# 3. 创建 Workflow
curl -X POST "${BASE_URL}/api/v1/workflow/publish" \
  -H "Authorization: Bearer ${API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"name":"test","dsl":{...}}'

# 4. 执行 Workflow
curl -X POST "${BASE_URL}/api/v1/workflow/run" \
  -H "Authorization: Bearer ${API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"workflowId":"..."}'

# 5. MCP 端点（Streamable HTTP）
curl -X POST "${BASE_URL}/v1/mcp" \
  -H "Authorization: Bearer ${API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"tools/list","id":1}'
```

### 2.3 SDK 使用（Python）

```python
import requests

ORIN_BASE_URL = "http://localhost:8080"
API_KEY = "sk-orin-your-key"

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json"
}

# 获取 Agent 列表
agents = requests.get(f"{ORIN_BASE_URL}/api/v1/agents", headers=headers)
print(agents.json())

# 执行 Workflow
result = requests.post(
    f"{ORIN_BASE_URL}/api/v1/workflow/run",
    headers=headers,
    json={"workflowId": "wf-xxx", "input": {"key": "value"}}
)
print(result.json())
```

---

## 3. MCP 集成

### 3.1 MCP 客户端配置

ORIN 作为 MCP Server，支持 Codex / Claude Desktop / Cursor / Windsurf 等客户端。

**Codex 配置（`~/.config/orin/config.json`）**：
```json
{
  "mcpServers": {
    "orin": {
      "url": "http://localhost:8080/v1/mcp",
      "headers": {
        "Authorization": "Bearer sk-orin-your-key"
      }
    }
  }
}
```

详细配置见 [docs/mcp-client-setup.md](./mcp-client-setup.md)。

### 3.2 MCP 工具列表

```bash
# 查看可用工具
curl -X POST "http://localhost:8080/v1/mcp" \
  -H "Authorization: Bearer sk-orin-your-key" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"tools/list","id":1}' | jq '.result.tools[].name'
```

可用工具：
- `agent.chat` — 与智能体对话
- `agent.list` — 列出所有 Agent
- `workflow.run` — 执行工作流
- `workflow.list` — 列出所有工作流
- `workflow.publish` — 发布新工作流

### 3.3 在代码中使用 MCP

```python
# orin-mcp-bridge 用法
from orin_mcp_bridge import load_config, create_server

config = load_config()  # 读取 ORIN_BASE_URL, ORIN_API_KEY
app = create_server(config)

# 桥接后 ORIN 作为 MCP Server 暴露所有工具
# 任何 MCP 客户端都可以通过 stdio 连接使用
```

---

## 4. 本地调试

### 4.1 后端调试

```bash
# 方式一：IDE 启动（推荐）
# 在 IDE 中直接运行 OrinBackendApplication.java，设置断点

# 方式二：Maven 启动
cd orin-backend
mvn spring-boot:run -Dspring-boot.run.arguments="--debug"

# 方式三：附加调试端口
java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005 \
     -jar target/orin-backend-1.0.0.jar
```

### 4.2 前端调试

```bash
cd orin-frontend

# 开发模式（热重载）
npm run dev

# 查看 Vue 组件 props / state
# 在浏览器 Vue DevTools 中打开

# API 请求抓包
# 浏览器 DevTools → Network → 过滤 /api/v1
```

### 4.3 AI Engine 调试

```bash
cd orin-ai-engine

# 激活虚拟环境
source .venv/bin/activate

# 启动（开发模式，reload=True）
uvicorn app.main:app --reload --port 8000 --log-level debug

# 查看 LangGraph 执行日志
# AI Engine 会输出每个节点的输入输出
```

### 4.4 Trace 调试

#### 4.4.1 Jaeger UI（Phase 2 小刀 5c 接入）

`docker compose up -d` 自动起 `jaeger` 服务（`jaegertracing/all-in-one:1.62`），
UI 暴露在 `http://localhost:16686`。

| 端口 | 协议 | 用途 | 服务 |
| --- | --- | --- | --- |
| 16686 | HTTP | Jaeger UI | jaeger |
| 4317 | OTLP gRPC | 后端 Java 上报 | orin-backend |
| 4318 | OTLP HTTP | AI Engine Python 上报 | orin-ai-engine |

UI 选 `Service` 下拉框会看到：
- `orin-backend`（Java，手动 init，gRPC OTLP）
- `orin-ai-engine`（Python，OTel SDK 1.42.1 + HTTP OTLP，Phase 2 小刀 5a/5b/5c）

#### 4.4.2 环境变量

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `ORIN_TRACING_ENABLED` | `true` | 总开关。`false` 时后端 `OpenTelemetryConfig` 跳过 TracerProvider；AI Engine 走 `OTEL_SDK_DISABLED=true` no-op 路径 |
| `OTEL_SERVICE_NAME` | `orin-backend` / `orin-ai-engine` | Jaeger UI 显示的 service 名（compose 已按 service 注入） |
| `OTEL_EXPORTER_OTLP_ENDPOINT` | `http://jaeger:4317` / `http://jaeger:4318` | OTLP endpoint，docker 内走 service name，本机脚本走 `localhost:4317` / `localhost:4318` |
| `OTEL_EXPORTER_OTLP_PROTOCOL` | `grpc` / `http/protobuf` | OTel SDK 自动读，与 endpoint 协议对齐 |
| `OTEL_SDK_DISABLED` | 未设 | AI Engine 侧：显式 `true` 完全 no-op（不建 TracerProvider），给 pytest / 本地调试用 |

#### 4.4.3 端到端 smoke

```bash
# 启 compose 后跑（5c 写）
./scripts/trace-smoke.sh

# 预期输出
#   services: ['orin-ai-engine', 'orin-backend']
#   span names: ['HTTP POST', 'workflow:execute', 'node:llm:...', ...]
#   trace_id: 0xabcdef...
#   Jaeger UI: http://localhost:16686/trace/0xabcdef...
```

#### 4.4.4 日志与 header

```bash
# 搜索特定 traceId 的日志
grep "traceId.*abc123def456" orin-backend/logs/orin-backend.json | jq .

# 查看 JSON 结构日志字段
head -1 orin-backend/logs/orin-backend.json | jq 'keys'

# 常见 trace 相关 header
# traceparent: 00-<traceId>-<spanId>-01  (W3C 标准，OTel SDK 1.42.x 默认 propagator)
# X-Trace-Id: <traceId>                   (legacy，Phase 2 小刀 3 已移除，W3C 取代)
```

#### 4.4.5 排查清单

- **Jaeger 0 spans**：compose 全 healthy？`docker compose logs orin-ai-engine | grep "tracing initialized"` 看 5a 启动日志
- **trace 缺 orin-ai-engine**：业务请求没穿透到 AI Engine（agent list 不调 ai-engine 走 backend 自己出 trace；agent chat / workflow execute 会触发后端 → ai-engine 出站）
- **trace 缺 orin-backend**：compose 里 `ORIN_TRACING_ENABLED=false` 关闭了后端 init
- **`AlreadyProvisionedError`**：OTel 1.42.x 进程级 TracerProvider 不可 reset，本地 uvicorn `--reload` 双 import 时会触发——属已知行为，重启进程即可

---

## 5. 测试

### 5.1 运行测试

```bash
# 后端单元测试
cd orin-backend && mvn test

# 后端带覆盖率
mvn test jacoco:report

# 前端测试
cd orin-frontend && npm run test

# AI Engine 测试
cd orin-ai-engine && pytest --cov=app --cov-report=term-missing
```

### 5.2 运行 Smoke 脚本

```bash
# Docker quickstart 健康检查
bash scripts/docker-smoke.sh

# 核心业务链路 API 级验收
bash scripts/business-smoke.sh

# Gateway MVP 基线（OpenAI 兼容 /v1/* + API Key 全生命周期）
bash scripts/gateway-smoke.sh

# MCP 协议验收（需要 API Key）
ORIN_API_KEY=sk-orin-xxx bash scripts/mcp-open-demo-smoke.sh
```

### 5.3 Playwright E2E

```bash
cd orin-frontend

# 运行 E2E 测试
npx playwright test

# 浏览器中打开 UI 并逐步执行
npx playwright test --ui

# 仅运行某个 spec
npx playwright test tests/e2e/collaboration-dashboard.spec.js
```

---

## 6. 代码规范

### 6.1 提交规范

```
<type>(<scope>): <subject>

Types: feat / fix / refactor / docs / test / chore
Scope: backend / frontend / ai-engine / docker / scripts / docs

示例：
feat(collaboration): add FALLBACK retry mechanism
fix(workflow): correct loop node output writing
docs(api): update API documentation for trace endpoint
```

### 6.2 分支规范

```
main          — 生产可发布代码
├── develop   — 开发主分支（如果使用 develop 分支）
├── feat/*    — 新功能
├── fix/*     — 修复
└── docs/*    — 文档
```

### 6.3 API 规范

- 所有 API 端点必须携带 `Authorization: Bearer <token/key>`
- 响应格式统一：`Result<T>`（包含 `code` / `message` / `data` / `traceId`）
- 错误响应必须包含 `traceId` 供排查
- 新接口必须加 Swagger `@Operation` 注解

详细规范见 [docs/开发规范.md](./开发规范.md)。

---

## 7. 常见问题

### Q: 后端启动报 `Database 'orindb' does not exist`

```bash
# 初始化数据库
docker compose up -d mysql
# Flyway 会自动执行 schema 初始化
# 如果是干净环境，确认 docker/mysql/init/01-orin-schema.sql 存在
```

### Q: AI Engine 调用返回 500

```bash
# 1. 检查 AI Engine 是否在线
curl http://localhost:8000/health

# 2. 检查 ORIN_AI_ENGINE_URL 配置
grep "ORIN_AI_ENGINE_URL" .env

# 3. 检查 AI Engine 日志
docker compose logs ai-engine --tail=30
```

### Q: MCP 工具调用返回 401

```bash
# 确认 API Key 正确且未过期
# 在管理台检查 Key 状态（启用/禁用）
# 确认 Authorization header 格式正确：Bearer sk-orin-xxx
```

### Q: traceparent 在 AI Engine 中看不到

```bash
# 确认 ORIN 后端已注入 traceparent
# 在 AI Engine 日志中搜索 traceparent
docker compose logs ai-engine | grep traceparent

# 或检查请求 header
curl -v http://localhost:8000/api/v1/run \
  -H "traceparent: 00-abcdef123456-1234567890abcdef-01"
```

---

*最后更新：2026-06-04*
